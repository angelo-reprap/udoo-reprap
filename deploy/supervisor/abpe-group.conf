; Optionale Gruppe: django + celery + scheduler-loop zusammen bedienen.
;   supervisorctl restart abpe:*
; Startet/stoppt alle drei; verhindert, dass nur django neu startet
; und der Loop vergessen wird.

[group:abpe]
programs=abpe-django,abpe-celery,abpe-scheduler-loop
priority=999
