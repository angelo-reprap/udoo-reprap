{
    "multiuser": "<h3>5.1 多用户架构 – 概述</h3><p>ABpE 门户设计用于支持数千名用户（预计超过 23,000 名顾问）。它利用 Django 成熟的认证和权限系统，并扩展了 ABpE 特定的数据模型。</p><h3>5.2 Django 用户模型</h3><p>Django 提供了一个功能完整的用户模型，提供所有基本的认证和授权功能：</p><pre><code>class User(models.Model):\n    username = models.CharField(unique=True)\n    password = models.CharField()  # 已哈希\n    email = models.EmailField()\n    first_name = models.CharField()\n    last_name = models.CharField()\n    is_active = models.BooleanField(default=True)\n    is_staff = models.BooleanField(default=False)\n    is_superuser = models.BooleanField(default=False)\n    date_joined = models.DateTimeField(auto_now_add=True)\n    last_login = models.DateTimeField(null=True)\n    groups = models.ManyToManyField(Group)\n    user_permissions = models.ManyToManyField(Permission)</code></pre><h3>5.3 顾问档案 – ABpE 扩展</h3><p>对于 ABpE 特定的数据，用户模型通过 <code>BeraterProfil</code> 进行扩展（一对一关系）：</p><pre><code>class BeraterProfil(models.Model):\n    user = models.OneToOneField(User, on_delete=models.CASCADE)\n    \n    # 标识\n    berater_id = models.CharField(max_length=20, unique=True)\n    titel = models.CharField(max_length=50, blank=True)  # 博士、教授等\n    \n    # 联系方式\n    telefon = models.CharField(max_length=50, blank=True)\n    mobil = models.CharField(max_length=50, blank=True)\n    adresse = models.TextField(blank=True)\n    linkedin = models.URLField(blank=True)\n    xing = models.URLField(blank=True)\n    \n    # 简历数据（以 JSON 格式存储，便于灵活处理）\n    cv_data = models.JSONField(default=dict)\n    cv_last_updated = models.DateTimeField(null=True, blank=True)\n    cv_public = models.BooleanField(default=False)\n    \n    # 状态与管理\n    status = models.CharField(max_length=20, default='in_pruefung')\n    betreuer = models.ForeignKey(User, on_delete=models.SET_NULL, null=True,\n                                  related_name='betreute_berater')\n    \n    # 时间戳\n    created_at = models.DateTimeField(auto_now_add=True)\n    updated_at = models.DateTimeField(auto_now=True)\n    last_activity = models.DateTimeField(null=True, blank=True)</code></pre><h3>5.4 CVVersion – 简历版本管理</h3><p>对简历数据的更改会进行版本控制，以保留历史记录并追踪更改：</p><pre><code>class CVVersion(models.Model):\n    berater = models.ForeignKey(BeraterProfil, on_delete=models.CASCADE,\n                                 related_name='cv_versions')\n    version = models.IntegerField()\n    cv_data = models.JSONField()\n    changed_by = models.ForeignKey(User, on_delete=models.SET_NULL, null=True)\n    change_note = models.CharField(max_length=255, blank=True)\n    created_at = models.DateTimeField(auto_now_add=True)\n    \n    class Meta:\n        unique_together = ['berater', 'version']\n        ordering = ['-version']</code></pre><h3>5.5 UserSettings – 用户设置</h3><p>对于与 UI 相关的设置，如主题和语言，存在 <code>UserSettings</code> 模型：</p><pre><code>class UserSettings(models.Model):\n    user = models.OneToOneField(User, on_delete=models.CASCADE)\n    theme = models.CharField(max_length=10, default='system')\n    language = models.CharField(max_length=5, default='de')\n    sidebar_collapsed = models.BooleanField(default=False)</code></pre><h3>5.6 角色与组</h3><p>权限结构通过 Django Groups 映射。定义了以下角色：</p><table class='doc-table'><tr><th>角色</th><th>访问权限</th><th>数量（估计）</th></tr><tr><td>顾问</td><td>自己的简历编辑器，自己的档案</td><td>~22,900</td></tr><tr><td>调度员</td><td>电子邮件收件箱，匹配，搜索/筛选顾问</td><td>~50</td></tr><tr><td>主管</td><td>其顾问的所有简历，创建/编辑顾问</td><td>~30</td></tr><tr><td>管理员</td><td>完全访问权限，用户管理，系统设置</td><td>~5</td></tr></table><h3>5.7 视图中的权限检查</h3><p>在视图中，使用 Django 装饰器检查权限：</p><pre><code>from django.contrib.auth.decorators import login_required, permission_required\n\n@login_required\ndef dashboard(request):\n    # 仅限已登录用户\n    ...\n\n@permission_required('cv_editor.view_own_cv')\ndef cv_editor(request):\n    # 仅限具有相应权限的用户\n    ...</code></pre><h3>5.8 模板中的权限检查</h3><p>在模板中也可以检查权限：</p><pre><code>{% if perms.cv_editor.view_own_cv %}\n    &lt;a href=\"{% url 'cv_editor' %}\"&gt;简历编辑器&lt;/a&gt;\n{% endif %}\n\n{% if user.is_staff %}\n    &lt;a href=\"/admin/\"&gt;管理后台&lt;/a&gt;\n{% endif %}</code></pre><h3>5.9 注册流程</h3><p>对于超过 23,000 名顾问，支持三种注册选项：</p><ul><li><strong>自助注册：</strong>顾问自行注册，管理员激活（或自动激活）</li><li><strong>CSV 导入：</strong>通过 Django 管理后台或管理命令批量导入</li><li><strong>API 集成：</strong>外部系统（CRM）通过 API 创建顾问</li></ul><h3>5.10 安全方面</h3><p>ABpE 门户依赖于 Django 成熟的安全机制：</p><ul><li>密码使用 PBKDF2 或 bcrypt 进行哈希处理（不存储明文）</li><li>所有 POST 表单的 CSRF 保护</li><li>通过模板自动转义实现 XSS 保护</li><li>通过 Django ORM 实现 SQL 注入保护</li><li>带有 HttpOnly、Secure、SameSite 标志的会话 Cookie</li><li>登录尝试的速率限制（django-axes）</li></ul>"
}