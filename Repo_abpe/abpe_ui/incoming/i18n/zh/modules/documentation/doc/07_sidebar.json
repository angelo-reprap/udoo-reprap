{
    "sidebar": "<h3>7.1 侧边栏与导航 – 概述</h3><p>侧边栏是 ABpE 门户的核心导航元素。它根据配置的模块和基础导航动态构建。侧边栏具有响应式设计，可适应不同的屏幕尺寸。</p><h3>7.2 侧边栏结构</h3><p>侧边栏由三个区域组成：</p><ol><li><strong>主导航：</strong>仪表盘 + 所有活动模块（来自 <code>module.json</code>）</li><li><strong>分隔线</strong>（视觉）</li><li><strong>系统导航：</strong>管理、API 文档、注销</li></ol><h3>7.3 导航的上下文处理器</h3><p>位于 <code>context_processors.py</code> 中的上下文处理器 <code>navigation()</code> 为侧边栏提供导航。它将来自 <code>modules.json</code> 的基础导航与扫描器中的模块相结合。</p><pre><code>def navigation(request):\n    # 加载基础导航\n    with open('modules.json') as f:\n        base_config = json.load(f)\n    \n    # 从扫描器加载模块\n    module_nav = scanner.get_navigation()\n    \n    # 主导航：仪表盘 + 模块\n    main_nav = [base_config['dashboard']] + module_nav\n    \n    # 系统导航\n    system_nav = base_config.get('system', [])\n    \n    return {\n        'main_navigation': main_nav,\n        'system_navigation': system_nav,\n    }</code></pre><h3>7.4 侧边栏模板</h3><p>侧边栏模板（<code>sidebar.html</code>）通用地渲染导航：</p><pre><code>&lt;div class=\"sidebar\" id=\"sidebar\"&gt;\n    &lt;div class=\"mt-3\"&gt;\n        {% for item in main_navigation %}\n        &lt;a href=\"{{ item.route }}\"\n           class=\"nav-link {% if active_module == item.id %}active{% endif %}\"\n           data-module=\"{{ item.id }}\"&gt;\n            &lt;i class=\"bi bi-{{ item.icon }}\"&gt;&lt;/i&gt;\n            &lt;span data-i18n=\"{{ item.id }}\"&gt;{{ item.title }}&lt;/span&gt;\n        &lt;/a&gt;\n        \n        {# 如果活动则显示子页面 #}\n        {% if item.subpages and active_module == item.id %}\n        &lt;div class=\"submenu ms-4\"&gt;\n            {% for subpage in item.subpages %}\n            &lt;a href=\"{{ subpage.route }}\" class=\"nav-link subpage-link\"&gt;\n                &lt;i class=\"bi bi-arrow-right-short\"&gt;&lt;/i&gt;\n                &lt;span&gt;{{ subpage.title }}&lt;/span&gt;\n            &lt;/a&gt;\n            {% endfor %}\n        &lt;/div&gt;\n        {% endif %}\n        {% endfor %}\n    &lt;/div&gt;\n    \n    &lt;hr class=\"mx-3\"&gt;\n    \n    &lt;div&gt;\n        {% for item in system_navigation %}\n        &lt;a href=\"{{ item.route }}\" class=\"nav-link\"&gt;\n            &lt;i class=\"bi bi-{{ item.icon }}\"&gt;&lt;/i&gt;\n            &lt;span&gt;{{ item.title }}&lt;/span&gt;\n        &lt;/a&gt;\n        {% endfor %}\n        &lt;a href=\"/logout/\" class=\"nav-link logout\"&gt;\n            &lt;i class=\"bi bi-box-arrow-right\"&gt;&lt;/i&gt;\n            &lt;span data-i18n=\"logout\"&gt;注销&lt;/span&gt;\n        &lt;/a&gt;\n    &lt;/div&gt;\n&lt;/div&gt;</code></pre><h3>7.5 高亮活动模块</h3><p>当前活动模块通过 CSS 类 <code>active</code> 高亮显示。视图必须在上下文中设置 <code>active_module</code> 值：</p><pre><code>return render(request, 'template.html', {\n    'active_module': 'dashboard',\n    ...\n})</code></pre><h3>7.6 子页面（子菜单）</h3><p>模块可以定义子菜单项（子页面）。当模块活动时，这些子页面会在侧边栏中显示在模块下方。子页面从模块的 <code>module.json</code> 中读取。</p><h3>7.7 移动端侧边栏</p><p>在移动设备上，侧边栏默认隐藏，可以通过按钮（汉堡菜单）显示。函数 <code>toggleMobileSidebar()</code> 负责显示和隐藏。</p><pre><code>function toggleMobileSidebar() {\n    const sidebar = document.getElementById('sidebar');\n    sidebar.classList.toggle('open');\n}</code></pre><p>相应的 CSS 确保侧边栏在移动设备上以覆盖层形式显示：</p><pre><code>@media (max-width: 768px) {\n    .sidebar {\n        left: -250px;\n        width: 250px;\n    }\n    .sidebar.open {\n        left: 0;\n    }\n}</code></pre>"
}