{
    "multiuser": "<h3>5.1 다중 사용자 아키텍처 – 개요</h3><p>ABpE 포털은 수천 명의 사용자(약 23,000명 이상의 컨설턴트) 운영을 위해 설계되었습니다. Django의 검증된 인증 및 권한 부여 시스템을 사용하며, ABpE 특화 데이터 모델로 확장됩니다.</p><h3>5.2 Django 사용자 모델</h3><p>Django는 인증 및 권한 부여를 위한 모든 기본 기능을 제공하는 완전한 기능의 사용자 모델을 제공합니다:</p><pre><code>class User(models.Model):\n    username = models.CharField(unique=True)\n    password = models.CharField()  # 해시됨\n    email = models.EmailField()\n    first_name = models.CharField()\n    last_name = models.CharField()\n    is_active = models.BooleanField(default=True)\n    is_staff = models.BooleanField(default=False)\n    is_superuser = models.BooleanField(default=False)\n    date_joined = models.DateTimeField(auto_now_add=True)\n    last_login = models.DateTimeField(null=True)\n    groups = models.ManyToManyField(Group)\n    user_permissions = models.ManyToManyField(Permission)</code></pre><h3>5.3 컨설턴트 프로필 – ABpE 확장</h3><p>ABpE 특화 데이터를 위해 사용자 모델은 <code>BeraterProfil</code>로 확장됩니다(일대일 관계):</p><pre><code>class BeraterProfil(models.Model):\n    user = models.OneToOneField(User, on_delete=models.CASCADE)\n    \n    # 식별\n    berater_id = models.CharField(max_length=20, unique=True)\n    titel = models.CharField(max_length=50, blank=True)  # Dr., Prof., 등\n    \n    # 연락처 정보\n    telefon = models.CharField(max_length=50, blank=True)\n    mobil = models.CharField(max_length=50, blank=True)\n    adresse = models.TextField(blank=True)\n    linkedin = models.URLField(blank=True)\n    xing = models.URLField(blank=True)\n    \n    # 이력서 데이터 (유연성을 위해 JSON으로)\n    cv_data = models.JSONField(default=dict)\n    cv_last_updated = models.DateTimeField(null=True, blank=True)\n    cv_public = models.BooleanField(default=False)\n    \n    # 상태 및 관리\n    status = models.CharField(max_length=20, default='in_pruefung')\n    betreuer = models.ForeignKey(User, on_delete=models.SET_NULL, null=True,\n                                  related_name='betreute_berater')\n    \n    # 타임스탬프\n    created_at = models.DateTimeField(auto_now_add=True)\n    updated_at = models.DateTimeField(auto_now=True)\n    last_activity = models.DateTimeField(null=True, blank=True)</code></pre><h3>5.4 CVVersion – 이력서 버전 관리</h3><p>이력서 데이터의 변경 사항은 기록을 유지하고 변경 사항을 추적할 수 있도록 버전 관리됩니다:</p><pre><code>class CVVersion(models.Model):\n    berater = models.ForeignKey(BeraterProfil, on_delete=models.CASCADE,\n                                 related_name='cv_versions')\n    version = models.IntegerField()\n    cv_data = models.JSONField()\n    changed_by = models.ForeignKey(User, on_delete=models.SET_NULL, null=True)\n    change_note = models.CharField(max_length=255, blank=True)\n    created_at = models.DateTimeField(auto_now_add=True)\n    \n    class Meta:\n        unique_together = ['berater', 'version']\n        ordering = ['-version']</code></pre><h3>5.5 UserSettings – 사용자 설정</h3><p>테마 및 언어와 같은 UI 관련 설정을 위해 <code>UserSettings</code> 모델이 있습니다:</p><pre><code>class UserSettings(models.Model):\n    user = models.OneToOneField(User, on_delete=models.CASCADE)\n    theme = models.CharField(max_length=10, default='system')\n    language = models.CharField(max_length=5, default='de')\n    sidebar_collapsed = models.BooleanField(default=False)</code></pre><h3>5.6 역할 및 그룹</h3><p>권한 구조는 Django Groups를 통해 매핑됩니다. 다음 역할이 정의되어 있습니다:</p><table class='doc-table'><tr><th>역할</th><th>접근 권한</th><th>수 (추정)</th></tr><tr><td>컨설턴트</td><td>자신의 이력서 편집기, 자신의 프로필</td><td>~22,900</td></tr><tr><td>디스패처</td><td>이메일 받은 편지함, 매칭, 컨설턴트 검색/필터링</td><td>~50</td></tr><tr><td>담당자</td><td>담당 컨설턴트의 모든 이력서, 컨설턴트 생성/편집</td><td>~30</td></tr><tr><td>관리자</td><td>전체 접근, 사용자 관리, 시스템 설정</td><td>~5</td></tr></table><h3>5.7 뷰에서의 권한 확인</h3><p>뷰에서는 Django 데코레이터를 사용하여 권한을 확인합니다:</p><pre><code>from django.contrib.auth.decorators import login_required, permission_required\n\n@login_required\ndef dashboard(request):\n    # 로그인한 사용자만\n    ...\n\n@permission_required('cv_editor.view_own_cv')\ndef cv_editor(request):\n    # 해당 권한이 있는 사용자만\n    ...</code></pre><h3>5.8 템플릿에서의 권한 확인</h3><p>템플릿에서도 권한을 확인할 수 있습니다:</p><pre><code>{% if perms.cv_editor.view_own_cv %}\n    &lt;a href=\"{% url 'cv_editor' %}\"&gt;이력서 편집기&lt;/a&gt;\n{% endif %}\n\n{% if user.is_staff %}\n    &lt;a href=\"/admin/\"&gt;관리자&lt;/a&gt;\n{% endif %}</code></pre><h3>5.9 등록 프로세스</h3><p>23,000명 이상의 컨설턴트를 위해 세 가지 등록 옵션이 지원됩니다:</p><ul><li><strong>자체 등록:</strong> 컨설턴트가 직접 등록하고 관리자가 승인(또는 자동)</li><li><strong>CSV 가져오기:</strong> Django 관리자 또는 관리 명령을 통한 대량 가져오기</li><li><strong>API 통합:</strong> 외부 시스템(CRM)이 API를 통해 컨설턴트 생성</li></ul><h3>5.10 보안 측면</h3><p>ABpE 포털은 검증된 Django 보안 메커니즘을 사용합니다:</p><ul><li>비밀번호는 PBKDF2 또는 bcrypt로 해시됨(평문 저장 없음)</li><li>모든 POST 양식에 대한 CSRF 보호</li><li>템플릿 자동 이스케이핑을 통한 XSS 보호</li><li>Django ORM을 통한 SQL 인젝션 보호</li><li>HttpOnly, Secure, SameSite 플래그가 있는 세션 쿠키</li><li>로그인 시도에 대한 속도 제한(django-axes)</li></ul>"
}