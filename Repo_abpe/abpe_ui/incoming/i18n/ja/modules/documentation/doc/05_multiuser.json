{
    "multiuser": "<h3>5.1 マルチユーザーアーキテクチャ – 概要</h3><p>ABpEポータルは、数千人のユーザー（推定23,000人以上のコンサルタント）による運用向けに設計されています。Djangoの実績ある認証・権限システムを利用し、ABpE固有のデータモデルで拡張されています。</p><h3>5.2 Djangoユーザーモデル</h3><p>Djangoは、認証と認可のためのすべての基本機能を提供するフル装備のユーザーモデルを提供します：</p><pre><code>class User(models.Model):\n    username = models.CharField(unique=True)\n    password = models.CharField()  # ハッシュ化\n    email = models.EmailField()\n    first_name = models.CharField()\n    last_name = models.CharField()\n    is_active = models.BooleanField(default=True)\n    is_staff = models.BooleanField(default=False)\n    is_superuser = models.BooleanField(default=False)\n    date_joined = models.DateTimeField(auto_now_add=True)\n    last_login = models.DateTimeField(null=True)\n    groups = models.ManyToManyField(Group)\n    user_permissions = models.ManyToManyField(Permission)</code></pre><h3>5.3 コンサルタントプロフィール – ABpE向け拡張</h3><p>ABpE固有のデータについては、ユーザーモデルが<code>BeraterProfil</code>（コンサルタントプロフィール）で拡張されています（1対1の関係）：</p><pre><code>class BeraterProfil(models.Model):\n    user = models.OneToOneField(User, on_delete=models.CASCADE)\n    \n    # 識別\n    berater_id = models.CharField(max_length=20, unique=True)\n    titel = models.CharField(max_length=50, blank=True)  # 博士、教授など\n    \n    # 連絡先\n    telefon = models.CharField(max_length=50, blank=True)\n    mobil = models.CharField(max_length=50, blank=True)\n    adresse = models.TextField(blank=True)\n    linkedin = models.URLField(blank=True)\n    xing = models.URLField(blank=True)\n    \n    # CVデータ（柔軟性のためJSON）\n    cv_data = models.JSONField(default=dict)\n    cv_last_updated = models.DateTimeField(null=True, blank=True)\n    cv_public = models.BooleanField(default=False)\n    \n    # ステータスと管理\n    status = models.CharField(max_length=20, default='in_pruefung')\n    betreuer = models.ForeignKey(User, on_delete=models.SET_NULL, null=True,\n                                  related_name='betreute_berater')\n    \n    # タイムスタンプ\n    created_at = models.DateTimeField(auto_now_add=True)\n    updated_at = models.DateTimeField(auto_now=True)\n    last_activity = models.DateTimeField(null=True, blank=True)</code></pre><h3>5.4 CVVersion – 履歴書のバージョン管理</h3><p>CVデータの変更はバージョン管理され、履歴を保持し変更を追跡できるようにします：</p><pre><code>class CVVersion(models.Model):\n    berater = models.ForeignKey(BeraterProfil, on_delete=models.CASCADE,\n                                 related_name='cv_versions')\n    version = models.IntegerField()\n    cv_data = models.JSONField()\n    changed_by = models.ForeignKey(User, on_delete=models.SET_NULL, null=True)\n    change_note = models.CharField(max_length=255, blank=True)\n    created_at = models.DateTimeField(auto_now_add=True)\n    \n    class Meta:\n        unique_together = ['berater', 'version']\n        ordering = ['-version']</code></pre><h3>5.5 UserSettings – ユーザー設定</h3><p>テーマや言語などのUI関連設定には、<code>UserSettings</code>モデルがあります：</p><pre><code>class UserSettings(models.Model):\n    user = models.OneToOneField(User, on_delete=models.CASCADE)\n    theme = models.CharField(max_length=10, default='system')\n    language = models.CharField(max_length=5, default='de')\n    sidebar_collapsed = models.BooleanField(default=False)</code></pre><h3>5.6 ロールとグループ</h3><p>権限構造はDjangoグループを介してマッピングされます。以下のロールが定義されています：</p><table class='doc-table'><tr><th>ロール</th><th>アクセス</th><th>数（推定）</th></tr><tr><td>コンサルタント</td><td>自身のCVエディタ、自身のプロフィール</td><td>~22,900</td></tr><tr><td>ディスパッチャー</td><td>メール受信箱、マッチング、コンサルタントの検索/フィルタリング</td><td>~50</td></tr><tr><td>担当者</td><td>担当コンサルタントの全CV、コンサルタントの作成/編集</td><td>~30</td></tr><tr><td>管理者</td><td>フルアクセス、ユーザー管理、システム設定</td><td>~5</td></tr></table><h3>5.7 ビューでの権限チェック</h3><p>ビューでは、Djangoデコレータを使用して権限がチェックされます：</p><pre><code>from django.contrib.auth.decorators import login_required, permission_required\n\n@login_required\ndef dashboard(request):\n    # ログインユーザーのみ\n    ...\n\n@permission_required('cv_editor.view_own_cv')\ndef cv_editor(request):\n    # 対応する権限を持つユーザーのみ\n    ...</code></pre><h3>5.8 テンプレートでの権限チェック</h3><p>テンプレートでも権限をチェックできます：</p><pre><code>{% if perms.cv_editor.view_own_cv %}\n    &lt;a href=\"{% url 'cv_editor' %}\"&gt;CVエディタ&lt;/a&gt;\n{% endif %}\n\n{% if user.is_staff %}\n    &lt;a href=\"/admin/\"&gt;管理&lt;/a&gt;\n{% endif %}</code></pre><h3>5.9 登録プロセス</h3><p>23,000人以上のコンサルタント向けに、3つの登録オプションがサポートされています：</p><ul><li><strong>自己登録：</strong> コンサルタントが自身で登録し、管理者が承認（または自動承認）</li><li><strong>CSVインポート：</strong> Django管理画面または管理コマンドによる一括インポート</li><li><strong>API統合：</strong> 外部システム（CRM）がAPIを介してコンサルタントを作成</li></ul><h3>5.10 セキュリティの側面</h3><p>ABpEポータルは、実績のあるDjangoセキュリティメカニズムに依存しています：</p><ul><li>パスワードはPBKDF2またはbcryptでハッシュ化（平文保存なし）</li><li>すべてのPOSTフォームに対するCSRF保護</li><li>テンプレートの自動エスケープによるXSS保護</li><li>Django ORMによるSQLインジェクション保護</li><li>HttpOnly、Secure、SameSiteフラグ付きのセッションCookie</li><li>ログイン試行のレート制限（django-axes）</li></ul>"
}